# Test Notes

The implementation plan calls for tests around normalization, ranking, navigation URLs, remembered access, Worker contracts, and mobile workflow behavior.

These tests are run with:

```sh
npm test
```

The first test pass should fail only when the corresponding implementation is missing; after each task implementation, keep the test suite green before moving to the next phase.
